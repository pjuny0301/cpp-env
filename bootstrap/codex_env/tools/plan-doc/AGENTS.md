# plan-doc Mirror Rules

- Start with `tool.json`.
- Read `README.md` second.
- Open `plan_doc.py` only when changing bundle generation or placeholder fallback behavior.
- Keep mirrored source aligned with `C:\codex-local\tools\plan-doc`.
