# External Build Material Rules

- Keep reusable libraries under `lib/`.
- Keep reusable tools under `tools/`.
- Keep user/editor setting templates under `user/`.
- Keep quiz external material under `quiz/`.
- Avoid storing generated build output here; use `../out/`.
