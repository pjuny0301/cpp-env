# C++ External Library Rules

- `desktop/` is for baseline C++ desktop dependencies and integration notes.
- `game/` is for game-specific C++ dependencies and integration notes.
- Keep dependency metadata small; only vendor source when the project explicitly needs an in-repo copy.
