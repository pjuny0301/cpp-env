# External Tool Rules

- `bootstrap/` contains the reusable Codex/build environment bootstrap.
- Add new reusable tools here only when they are shared across projects.
- App-specific tools should stay under the app workspace unless they become reusable.
