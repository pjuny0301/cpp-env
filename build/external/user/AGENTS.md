# User Setting Template Rules

- `editor/vscode/` is for reusable editor setting templates.
- Keep machine-specific paths out of committed templates unless the path is part of the shared contract.
- Prefer small settings files and README notes over copying full editor state.
