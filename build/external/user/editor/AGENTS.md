# Editor Setting Rules

- `vscode/` is for reusable VS Code setting templates.
- Keep user-private paths and tokens out of editor templates.
- Prefer small settings snippets over full editor state copies.
