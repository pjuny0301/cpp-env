# Codex Common Rules

- This folder holds reusable Codex material shared across projects.
- `agent/` contains shared handoff and token-budget metadata.
- `mcp/` contains common MCP-related local material.
- Do not place app-specific requirements here; put those under `../projects/<project>/`.
- Keep files small and metadata-focused so future agents can read them before touching large app folders.
