# Agent Subtree Rules

- Read `shared_command.json` first.
- Read `token_budget.json` second.
- Keep edits in this subtree small and metadata-focused.
- Use this subtree for shared handoff, token-budget, and multi-agent coordination files only.
- Do not place large artifacts or generated binaries here.
- If you need broader repo context, move back to the root `AGENTS.md` instead of expanding this subtree.
