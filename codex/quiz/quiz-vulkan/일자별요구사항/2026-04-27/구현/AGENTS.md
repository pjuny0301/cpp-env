# Implementation Requirement File Rules

- 파일명은 요구사항 번호를 기준으로 `01.md`, `02.md`처럼 두 자리로 쓴다.
- 중복 번호는 `73a.md`, `73b.md`처럼 suffix를 붙인다.
- 각 파일은 `요구사항 원문`, `구현 방식`, `관련 경로`, `대안`, `장단점`, `검증` 섹션을 유지한다.
- 구현 경로가 바뀌면 이 파일과 상위 `requirements.md`를 같이 갱신한다.
