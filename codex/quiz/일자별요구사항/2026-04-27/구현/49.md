# Requirement 49

## 요구사항 원문

49\. 장문 문제인 경우와 짧은 문제를 나눈다(이미 나눠진다면 장문 기준을 더 짧게 한다.


\[50번부터 56번까지는 다른 브랜치에서 실시]

## 구현 방식

장문/짧은 문제 구분은 `question_length_class`를 계산하거나 명시 필드로 둔다. 이미 나뉘는 경우 threshold를 낮춰 장문 layout이 더 빨리 적용되게 하고, long text font size와 scroll policy를 별도 테스트한다.

## 관련 경로

- `apps/quiz/android-quiz-app`, `apps/quiz/quiz-vulkan`
- `codex/quiz/big_plan.md`
- `codex/quiz/big_plan.md`

## 대안

React UI에서만 처리하지 말고 domain/FSM 규칙으로 먼저 고정한다.

## 장단점

장점: 포팅과 검증이 쉬워진다. 단점: 초기에 문서와 계약을 먼저 맞추는 비용이 든다.

## 검증

domain unit test와 주요 UI flow smoke test로 검증한다.
