# Plan Bundle Rules

- Read `bundle_manifest.json` first.
- Read `prompt_overrides.json` second.
- Read `render_spec.json` or `prompts.json` only when needed.
- Avoid `plan_summary.docx` unless the task is layout or rendering verification.
- If `placeholder_notice.txt` exists, surface that to the user before discussing image quality.
